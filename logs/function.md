# Function

## 학습일

- 2026-07-07

## Goal
- 함수 정의와 호출을 이해한다.
- return과 print의 차이를 이해한다.

## Implementation
파일: exercises/day01_function.py

구현 내용
- greet(name)
- introduce(name, age)
- multiply(a, b)
- calculate(a, b)

핵심 학습
- return을 이용해 값을 반환하는 함수를 작성했다.
- 하나의 문자열을 만들어 출력하는 방식으로 결과를 구성했다.

## Learned
- print는 화면 출력이다.
- return은 값을 반환한다.
- 함수는 역할별로 분리하는 것이 좋다.

## Next
- 파일 입출력(File I/O) 학습